{
  "name": "zoompilot-wiki",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "cp contrib/_headers site/_headers && wrangler pages deploy site --project-name zoompilot-wiki --branch main"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
